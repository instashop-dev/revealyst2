/**
 * @revealyst/vectorize — Revealyst Cloudflare Vectorize integration.
 *
 * Phase 1 scaffold: namespace config, pattern dataset and seed scripts land in Phase 3.
 */
export const vectorizeNamespace = "prompt-patterns";
export const vectorizeDimensions = 1536;
